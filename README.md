# bun-tree-shake-repro

To install dependencies:

```bash
bun install
```

To repro:

```bash
bun run repro
```

This project was created using `bun init` in bun v1.1.10. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.
